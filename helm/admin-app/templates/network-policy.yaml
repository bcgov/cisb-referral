apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cisb-referral-admin-app-network-policy
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: cisb-referral-admin-app
  policyTypes:
    - Ingress
  ingress:
    # Allow health probes from kubelet and all external traffic
    - ports:
        - protocol: TCP
          port: 3000
        - protocol: TCP
          port: 3001
